[project]
name = "databricks-data-engineering-lab"
version = "0.1.0"
description = "Production-like data engineering lab using Python, PySpark and Databricks."
requires-python = ">=3.11,<3.12"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]